import 'package:e_waste_app/main.dart';
import 'package:flutter/material.dart';

class ReqFormToHub extends StatelessWidget {
  const ReqFormToHub({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Place Request"),
      ),
      body: const Center(
        child: Text("Form here"),
      ),
    );
  }
}
